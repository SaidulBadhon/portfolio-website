PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/portfolio

# Shared secret required for every write/admin endpoint (create/update/delete
# and reading contact messages). Use the same value in apps/web.
# Generate one with: openssl rand -hex 32
API_SECRET=
